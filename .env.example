# Database Configuration (Required for Backend)
GOOGLE_CREDENTIALS=your_google_service_account_json
# OR use a file: credentials.json

# API Configuration (Optional - Defaults to localhost:5000)
# For Frontend:
VITE_API_URL=http://localhost:5000

# Server Configuration (Optional - Defaults to 5000)
PORT=5000
